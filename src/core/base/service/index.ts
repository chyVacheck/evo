/**
 * @file index.ts
 * @module core/base/service
 *
 * @description
 * Сборный файл для экспорта всех базовых классов сервисов из директории modules.
 * Удобен для импорта в других модулях, когда требуется доступ к нескольким базовым классам сервисов.
 */

export * from './Service.module';
export * from './MongoService.module';
