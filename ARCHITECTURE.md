# 🧠 Архитектура Evo

## Поток запроса

1. HTTP-запрос принимается `App` и направляется в `RouterModule`.
2. `RouterModule` сопоставляет маршрут, метод и вызывает нужный контроллер.
3. Перед вызовом контроллера выполняется цепочка before middleware:
   - `ContentTypeMiddleware`
   - `ParseBodyMiddleware`
4. Контроллер получает `HttpContext` с типизированными данными и вызывает сервис.
5. Сервис возвращает `ServiceResponse`, контроллер который оборачивает его в `SuccessResponse` или `ErrorResponse`.

---

## Слои проекта

### core/

Базовые системные классы:

- `ControllerModule` — логика контроллеров;
- `BeforeMiddlewareModule` — реализация before-middleware;
- `RequestContext` — контейнер состояния запроса;
- `HttpContext` — контекст HTTP-уровня.

### modules/

Бизнес-модули приложения (`user`, `todo`):

- `*.router.ts` — входная точка для HTTP, где подключаются необходимые мидлвары;
- `*.controller.ts` — основной обработчик запроса;
- `*.service.ts` — бизнес-логика;
- `*.repository.ts` — работа с БД.

### utils/

Вспомогательные утилиты, к примеру `StringUtils`.

---

## Принципы

- Все зависимости передаются явно.
- Нет магии, DI или рефлексии.
- Максимальная прозрачность между слоями.
