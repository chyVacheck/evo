# 🧬 Evo

> **The next evolution of web frameworks.**  
> Fast. Typed. Minimal. Built for the future.

Evo — это современный backend-фреймворк, созданный на основе **Bun**.  
Он сочетает **скорость нативного Bun.serve**, **типобезопасность TypeScript**  
и **архитектурную прозрачность**, вдохновлённую Koa и Elysia,  
чтобы ты мог строить лёгкие, масштабируемые и понятные API-сервера.

---

## 🚀 Основные принципы

- ⚡ **Скорость:** работает поверх Bun.serve без потери производительности.  
- 🧩 **Модульность:** вложенные роутеры, middleware и контексты любого уровня.  
- 🔒 **Типобезопасность:** полное выведение типов контекста и middleware.  
- 🧠 **Прозрачность:** без скрытой магии — только контролируемое поведение.  
- 🧰 **Расширяемость:** можно использовать как микрофреймворк или как основу целой платформы.

---

## 🛠️ Пример кода

```ts
import { Evo } from "evo"

const app = new Evo()

app.get("/", () => new Response("Hello from Evo!"))

app.listen(3000)
```

**Результат:**  
```
$ bun run dev
Listening on http://localhost:3000
```

---

## 🧩 Возможности

| Категория | Описание |
|------------|-----------|
| 🔹 **Роутер** | Radix-tree структура (O(1) поиск), поддержка параметров `/users/:id` и query-параметров |
| 🔹 **Middleware** | before / after / onError-хуки, типобезопасные цепочки |
| 🔹 **Nested-роутеры** | вложенные роутеры: `/users/:id/avatar` обрабатывается в под-модуле |
| 🔹 **Типизация контекста** | после middleware контроллеры автоматически «знают» об `ctx.requestId` и других данных |
| 🔹 **Ответы** | удобные хелперы: `ctx.json()`, `ctx.text()`, `ctx.send()` |
| 🔹 **Ошибки и заголовки** | глобальные обработчики ошибок, автоматическая установка корректных HTTP-заголовков |
| 🔹 **WebSocket / SSE** | встроенная поддержка событийных соединений (в разработке) |

---

## 🧬 Архитектура

```
src/
 ├─ core/            # ядро фреймворка: сервер, типы, контекст
 ├─ router/          # система маршрутизации (radix-tree)
 ├─ middleware/      # встроенные middleware
 ├─ utils/           # утилиты и помощники
 └─ index.ts         # экспорт класса Evo
```

---

## 🧩 Планы развития

- ✅ Роутер с O(1) поиском (radix-tree)
- ✅ Middleware-цепочка с типобезопасностью
- ⏳ Nested-роутеры
- ⏳ RequestContext
- ⏳ Zod-валидация и автогенерация типов
- ⏳ WebSocket / SSE
- ⏳ CLI и документация
- ⏳ OpenAPI-генерация и Swagger-интерфейс

---

## 📦 Установка

```bash
bun add evo
```

или (для локальной разработки фреймворка):

```bash
git clone https://github.com/yourusername/evo.git
cd evo
bun install
```

---

## ⚡ Быстрый старт

```bash
mkdir my-api
cd my-api
bun init
bun add evo
```

**index.ts:**
```ts
import { Evo } from "evo"

const app = new Evo()

app.get("/hello", () => Response.json({ message: "Hello from Evo" }))

app.listen(8080)
```

**Результат:**
```
Listening on http://localhost:8080
```

---

## 🧠 Почему Evo

> Because frameworks should evolve — not grow heavy.

Evo стремится к идеалу:  
**скорость на уровне Bun**,  
**архитектурная прозрачность Koa**,  
и **удобство типизации, как у Elysia**.

---

## 🧭 Лицензия
MIT © 2025
